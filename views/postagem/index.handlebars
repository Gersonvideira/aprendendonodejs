
<div class="md-4">
    <h1>{{postagem.titulo}}</h1>
</div>

<div class="card">
    <div class="card-body">
        <small>Data da publicação: {{postagem.data}}</small>
<hr>

{{postagem.conteudo}}

    </div>
</div>